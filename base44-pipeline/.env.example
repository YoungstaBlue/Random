# base44 pipeline configuration — copy to `.env` and fill in.
# Every value is optional; unset integrations no-op cleanly instead of crashing.
# All variables are prefixed BASE44_ and read by base44/config.py.

# ---- Module 1: Core Data & Storage ----
BASE44_LOCAL_DB_PATH=./base44_data/base44.db
BASE44_BACKUP_DIR=./base44_data/backups
# 32-byte urlsafe-base64 Fernet key for encrypted offline backups.
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
BASE44_BACKUP_KEY=
# Google Drive sync (service-account JSON path + destination folder id).
BASE44_GDRIVE_CREDENTIALS_JSON=
BASE44_GDRIVE_FOLDER_ID=

# ---- Module 2: Processing, Integration & Routing ----
BASE44_API_HOST=127.0.0.1
BASE44_API_PORT=8044
# Comma-separated outbound webhook URLs notified on data changes.
BASE44_WEBHOOK_URLS=

# ---- Module 3: AI Analysis & Verification ----
BASE44_ANTHROPIC_API_KEY=
BASE44_ANTHROPIC_MODEL=claude-opus-4-8

# ---- Block A: Vector-Matrix engine tuning ----
BASE44_TFIDF_MAX_FEATURES=50000
BASE44_TFIDF_NGRAM_MAX=2
BASE44_LSH_NBITS=256
BASE44_QUERY_TOP_K=10
